## Work In Progress Log

- 2026-07-05: First setup of the Take-AI-Control-Back repository (GG)
- 2026-07-07: Security fix — container no longer runs as root. Added `devcontainer` user (uid 1000), moved installs to system paths, enabled `remoteUser` in devcontainer.json. User has passwordless sudo for when root is needed. (Copilot CLI / Opus 4.6)
- 2026-07-07: Node.js install simplified via multi-stage build (`COPY --from=node:22-slim`). Removes manual curl/tar/arch logic entirely. (Copilot CLI / Opus 4.6)
- 2026-07-07: Added runInContainer.sh (GG)
- 2026-07-07: Upgraded Node.js donor stage from node:22-slim to node:24-slim in Dockerfile. Single line change, no other modifications. (claude-sonnet-4-5)
- 2026-07-08: Security review identified 7 issues: passwordless sudo, unquoted shell vars, host credential mounts, env file tracking, inconsistent --ignore-scripts, unpinned RTK version, auth.json in workspace. No critical vulnerabilities. (Kiro/Auto/Claude)
- 2026-07-08: Pinned RTK version to v0.43.0 in Dockerfile for reproducible builds. (Kiro/Auto/Claude)
- 2026-07-08: Investigated --ignore-scripts for claude-code and copilot: both require postinstall scripts to download platform-specific native binaries. Added comment documenting why the flag cannot be used. (Kiro/Auto/Claude)
- 2026-07-08: Fixed issue #3 (host credentials mounted read-write) — .copilot volumes now mounted :ro. Fixed issue #4 (env file tracking) — added explicit .devcontainer/devcontainer.env to .gitignore and .dockerignore. Also quoted all shell variables in runInContainer.sh. (Kiro/Auto/Claude)
- 2026-07-10: Created doc/crc-cards.md — comprehensive CRC Cards (Class-Responsibility-Collaborator) reference covering history, card layout, process, rules of thumb, example, comparisons with related techniques, strengths/limitations, facilitation tips, and key references. (pi / claude-sonnet-4-5)
- 2026-07-10: Created doc/ai-sdlc-howto.md — AI-SDLC operating manual. Covers all 7 SDLC phases with tools, prompt patterns, CI/CD integration, autonomous agents, risk mitigation (hallucinations, security, IP, prompt injection), NIST AI RMF governance checklist, and DORA metrics. Researched via researcher subagent. (pi / claude-sonnet-4-5)
- 2026-07-13: WIP — created `.codex/skills/convert-with-markitdown` with a `.venv`-only wrapper, pinned local-format and HTTP dependencies, opt-in HTTPS fetching with SSRF/redirect/timeout/size guards, opt-in plugins, and output overwrite protection. Remaining by design: Azure, LLM/OCR, MCP, audio, YouTube, and batch support are excluded. (Codex / GPT-5)
- 2026-07-13: WIP — generalized `convert-with-markitdown` as a shared Agent Skill under `.agents/skills`, with native discovery paths for Codex and Claude Code; GitHub Copilot discovers the canonical path directly. Remaining by design: the runtime is repository-local and still requires `.venv` plus RTK. (Codex / GPT-5)
- 2026-07-15: WIP — container reproducibility + host UID/GID alignment. Pinned base images by digest (`node:24-slim`, `python:3.14-slim-trixie`) and pinned global npm tools (`@earendil-works/pi-coding-agent@0.80.7`, `@anthropic-ai/claude-code@2.1.210`, `@github/copilot@1.0.70`). Added Docker build args `DEV_UID`/`DEV_GID`, updated user creation to honor them, and passed args from both `bin/runInContainer.sh` and `.devcontainer/devcontainer.json`. Verified with `docker build` and local image inspect. Remaining: optional follow-up to pin transitive Python dependencies via lock+hashes and to tighten sudo/mount policy. (GitHub Copilot / GPT-5.3-Codex)
- 2026-07-15: GG Solved problem of locale using not-slim image.
- 2026-07-18: Proposed OpenSpec change `add-nello-frontend` — nello (Trello-clone demo) frontend in React+TypeScript. Created all 4 artifacts (proposal, design, 4 delta specs: board-management, list-management, card-management, board-persistence, tasks); change validates clean. Decisions: Vite + strict TS under `nello/frontend/`, useReducer+Context normalized store, @dnd-kit for DnD, versioned localStorage persistence, Vitest+RTL. Remaining: implementation via `/opsx:apply`. (Claude Code / deepseek-v4-pro[1m])
- 2026-07-19: Implemented `add-nello-frontend` — 27/27 tasks complete. Scaffolded Vite react-ts app under `nello/frontend/`, built normalized reducer (8 actions → 31 tests), localStorage persistence (versioned, debounced write-through), board/list/card UI with inline editing and confirm dialogs, drag & drop (card reorder/cross-list move + list reorder via @dnd-kit), smoke tests (create board→list→card flow, persistence reload). All 40 tests pass, `tsc --noEmit` clean, production build succeeds (~253 KB gzipped). Updated README with dev commands. (Claude Code / deepseek-v4-pro[1m])
- 2026-07-20: Implemented `add-shared-boards` — 33/33 tasks complete. Board sharing via `$` name suffix convention: `board_member` junction table, `check_board_access()` authorization helper returning "owner"/"member"/None, UNION query for board listing (owned + member boards), `$` suffix is permanent (rename rejection with 409), only owner manages members and deletes board. Card tracking: `modified_by` column on cards set on create/update/move, returned as `modifiedBy` in responses. New member management module (POST/DELETE/GET endpoints). Response changes: `BoardResponse` gains `isShared`/`isOwner`, `CardResponse` and `CardBrief` gain `modifiedBy`. All 58 backend tests pass, frontend unaffected (43 tests, tsc clean). (Claude Code / deepseek-v4-pro[1m])
- 2026-07-20: Implemented `add-shared-boards-frontend` — 23/23 tasks complete. Updated `api.ts` types (BoardBrief, CardBrief, CardResponse) and added member API functions (addMember, removeMember, listMembers). Extended state types (Board.isShared/isOwner, Card.modifiedBy) and reducer to store them. Updated loadBoards to pass new fields through from API. Created MemberDialog component with member list, add-by-email form, and remove buttons. Added 👤 share button to BoardSwitcher on owned shared boards. CardModal now shows "Last modified by: <user_id>" when modifiedBy is set. All 43 frontend tests pass, TypeScript clean, 58 backend tests still passing. (Claude Code / deepseek-v4-pro[1m])
- 2026-07-20: WIP — reviewed `nello/frontend` without modifying application code. Identified optimistic-update divergence on failed mutations, ignored API response metadata for newly created/edited boards and cards, unsynchronized auth state after HTTP 401, duplicate card-save requests, and missing member-operation success feedback. Remaining: implement fixes and add regression coverage; local verification could not run because frontend dependencies are not installed (`vitest`, `oxlint`, and `tsc` unavailable). (Codex / GPT-5)
- 2026-07-20: WIP — fixed nello frontend P1/P2 review findings. Removed the unused member-dialog binding that blocked strict TypeScript builds; synchronized HTTP 401 responses with React auth state; reconciled board/card mutation metadata; restored authoritative server state after failed optimistic mutations and card moves; hid owner-only deletion from board members; added regression tests. Verified 47 tests, production build, and lint (two existing Fast Refresh warnings remain). Remaining P3 review items: duplicate card-modal saves and member-operation success feedback. (Codex / GPT-5)
- 2026-07-20: WIP — proposed OpenSpec change `add-help-box`. Created proposal, design, `in-app-help` capability spec, and six implementation tasks for an authenticated lower-right help box with shared-board guidance, responsive presentation, accessible controls, and versioned browser-local dismissal. OpenSpec validation passes. Remaining: add the final help copy supplied by the user, then implement via `/opsx:apply`. (Codex / GPT-5)
- 2026-07-20: WIP — implemented OpenSpec change `add-help-box`. Added an accessible, responsive fixed lower-right help box for authenticated users with shared-board and member-management guidance, versioned local-storage dismissal, and safe in-memory fallback. Added coverage for visibility, login exclusion, dismissal, stale versions, and storage failure. Verified 51 frontend tests and production build. Remaining: add future help entries by updating `HelpBox.tsx` and incrementing its content version for material new guidance. (Codex / GPT-5)
- 2026-07-20: WIP — proposed OpenSpec change `add-fast-card-entry-via-keyboard`. Created proposal, design, `card-management` delta spec, and five tasks for list-body activation, consecutive Enter card creation, preserved existing interactions, and UI verification. OpenSpec validation passes. Remaining: implementation via `/opsx:apply`. (Codex / GPT-5)
- 2026-07-20: WIP — implemented OpenSpec change `add-fast-card-entry-via-keyboard`. Clicking unused list-body space now opens and focuses the card composer; Enter creates consecutive cards without closing it, while Shift+Enter, Escape, explicit Add Card, card clicks, and list-title renaming retain their behaviors. Empty lists have a larger click target. Added UI tests and updated help-box test labels to match the current user-edited accessible heading. Verified 53 frontend tests and production build. Remaining: archive the completed change when ready. (Codex / GPT-5)
- 2026-07-20: WIP — archived OpenSpec change `add-fast-card-entry-via-keyboard` after syncing its card-creation requirement into the main `card-management` spec. The archive is complete; remaining work is limited to future feature changes. (Codex / GPT-5)

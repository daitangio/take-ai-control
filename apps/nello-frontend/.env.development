# Backend API base URL for local development.
# The Vite proxy can be unreliable; pointing directly to the backend
# is more robust. CORS is open on the backend (SecurityConfig).
VITE_API_URL=http://localhost:8080
